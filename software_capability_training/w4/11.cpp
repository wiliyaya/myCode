#include <iostream>
using namespace std;
//a[i]
//last代表前一天在干嘛{0，1，2}休息，健身房，竞赛
//如果(a[i]==1 || a[i]==3) && last!=2 可以竞赛
//如果(a[i]==2 || a[i]==3) && last!=1 可以健身
//如果a[i]==0 只能休息 
//f(i)代表到第i天为止最少的休息天数
//f(i)=f(i-1)+是否休息
//裂开了，不会
void f(){

}

int main(){

}